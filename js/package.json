{
    "name": "user_vo-admin-js",
    "private": true,
    "description": "Test tooling for js/admin.js. Not used at runtime - Nextcloud loads admin.js as a plain <script>.",
    "scripts": {
        "test": "jest"
    },
    "devDependencies": {
        "jest": "^29.7.0",
        "jest-environment-jsdom": "^29.7.0",
        "moment": "^2.30.1"
    },
    "jest": {
        "testEnvironment": "jsdom",
        "setupFiles": [
            "./tests/setup.js"
        ],
        "testMatch": [
            "<rootDir>/tests/**/*.test.js"
        ]
    }
}
