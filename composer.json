{
    "name": "bkhoesie/user_vo",
    "description": "Static analysis tooling for user_vo. Not used for runtime autoloading - Nextcloud's own classloader handles that via appinfo/info.xml.",
    "license": "AGPL-3.0-or-later",
    "require-dev": {
        "vimeo/psalm": "^5.26",
        "nextcloud/ocp": "dev-stable34"
    },
    "config": {
        "sort-packages": true
    }
}
